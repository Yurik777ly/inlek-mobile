<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- 🔹 Кастомная тема для JivoSDK -->
    <style name="JivoSDKCustomTheme" parent="Base.Theme.JivoSDK">
        <!-- Цвета -->
        <item name="colorPrimary">#3d4360</item>
        <item name="colorPrimaryVariant">#3d4360</item>
        <item name="colorAccent">#3d4360</item>

        <!-- Статус-бар -->
        <item name="colorPrimaryDark">#3d4360</item>               <!-- для старых API -->
        <item name="android:statusBarColor">#3d4360</item>         <!-- для современных -->
        <item name="android:windowLightStatusBar">false</item>     <!-- или true, если нужны тёмные иконки -->

        <!-- Навигационная панель -->
        <item name="android:navigationBarColor">#000000</item>

        <!-- Не рисуем под системными панелями -->
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="JivoSDKThemeSwitcher">
        <item name="theme">@style/JivoSDKCustomTheme</item>
    </style>
</resources>
